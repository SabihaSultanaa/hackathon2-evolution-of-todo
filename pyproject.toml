[project]
name = "todo-app"
version = "0.1.0"
description = "An interactive menu-driven Todo CLI application"
requires-python = ">=3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.uv]
dev-dependencies = ["pytest"]
